# For more information about configuring kind deployments, see here: https://kind.sigs.k8s.io/docs/user/configuration/#runtime-config
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
#  - extraPortMappings:
#    - containerPort: 30000
#      hostPort: 30000
#      protocol: TCP